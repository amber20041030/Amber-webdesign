# 自訂網站編輯器 v146

本版延續 v145，新增「匯出備份編輯器」功能。

## v146 修改重點

- 新增頂部按鈕：匯出備份編輯器。
- 匯出的備份 ZIP 會包含可繼續編輯的 `index.html`。
- 同時輸出 `backup-project.json`，可用原本的「匯入備份」載回。
- 備份編輯器會自動載入匯出當下的所有頁面、元件、樣式、動畫、下拉換組、多選設定等資料。
- 若同一瀏覽器已有較新的手動存檔，開啟備份編輯器時會保留較新的資料，避免舊備份覆蓋。
- 不影響原本「匯出 ZIP」前台網站功能。
